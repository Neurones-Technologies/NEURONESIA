"""Analyses IA de cockpit calculées une fois par jour et figées en base.

Voir `store.py` (persistance + lecture du jour), `computations.py` (les 7
narrations et les chiffres réels qu'elles commentent), `service.py` (génération
complète appelée par le job planifié) et `router.py` (statut/relance).
"""
